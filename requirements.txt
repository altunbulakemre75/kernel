# kernel — Python dependencies
# Core web framework
fastapi==0.115.0
uvicorn[standard]==0.31.0
pydantic==2.9.2
pydantic-settings==2.5.2

# Database (PostgreSQL + TimescaleDB)
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.13.3

# Auth (JWT + password hashing)
# PyJWT (maintained, timezone-aware) replaced python-jose which used utcnow()
# Upper bound pinned so a PyJWT 3.x breaking change can't silently land at install time.
PyJWT>=2.9.0,<3.0.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.12

# HTTP client
httpx==0.27.2

# Config
python-dotenv==1.0.1

# AI / ML
numpy>=1.24.0,<2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0
# torch installed separately in Dockerfile via CPU-only wheel index
# (default torch wheel pulls ~800MB of CUDA libs we don't use)

# Messaging
nats-py>=2.6.0,<3.0.0

# OpenTelemetry (optional — gracefully disabled if not installed)
# Upper bounds keep the three OTEL packages on the same 1.x minor release;
# skew across them causes subtle ABI breaks.
opentelemetry-sdk>=1.25.0,<2.0.0
opentelemetry-exporter-otlp-proto-grpc>=1.25.0,<2.0.0
opentelemetry-instrumentation-fastapi>=0.46b0,<1.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.23.0

# ── kernel services ───────────────────────────────────────────────

# Sensor processing (optional — install for camera pipeline)
# ultralytics>=8.3.0,<9.0.0   # large dep; enable only if vision pipeline active
# opencv-python-headless       # only needed with ultralytics

# Fusion
filterpy>=1.4.5,<2.0.0
scipy>=1.11.0
# pyproj — optional. Without it shared/geo.py falls back to flat-Earth math.
# Install when multi-site or >20 km range is needed.
pyproj>=3.6.0

# Decision policy (YAML rules)
pyyaml>=6.0.0

# Observability
prometheus-client>=0.19.0

# Optional LLM stack — install with:
#   pip install "langgraph>=0.2.0" "anthropic>=0.40.0" "llama-index>=0.12.0"
# Required when KERNEL_DECISION_LLM_ENABLED=true.
